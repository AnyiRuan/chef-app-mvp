# V1.0 UI/UX 设计规范 (UI Design Specification)

结合“智能厨艺导航”项目定位（面向普通家庭、破除“手忙脚乱”、科技感），我们对 V1.0 版本的核心 UI 视觉和交互规范定义如下：

## 1. 整体视觉风格 (Visual Aesthetic)
*   **深色模式优先 (Dark Mode First)**：厨房光线复杂且可能有油污反光，深色背景（深邃灰/曜石黑）能让亮色的文字和食材图片更加凸显，减轻视觉疲劳。
*   **毛玻璃拟物化 (Glassmorphism)**：大量使用半透明卡片和背景虚化效果。这不仅具有强烈的现代感和高级感，还能让底部的食物背景图若隐若现，增加食欲感。
*   **流体动画 (Fluid Micro-animations)**：拒绝生硬跳转。步骤切换要有丝滑的滑动过渡；倒计时有呼吸闪烁感；按钮点击带有水波纹或微缩放效果，让 APP “活起来”。

## 2. 色彩系统 (Color Palette)
*   **基础底色 (Background)**：`#0F1014` (极夜黑) 搭配深色渐变。
*   **主要强调色 (Primary)**：`#FF6B6B` (暖艳红)。常用于开始按钮、重要警告（如大火提示），能迅速引起注意并刺激食欲。
*   **次要强调色 (Secondary)**：`#FCA311` (温暖橙)。用于进行中的倒计时环、进行中的状态，给人以温度和期待感。
*   **成功/完成色 (Success)**：`#4ECDC4` (薄荷绿)。代表一个步骤完美结束、倒计时归零。
*   **核心文字 (Text)**：`#FFFFFF` (纯白，大号字为主) / `#A0A0B0` (次要说明文字，减轻对比度刺眼)。

## 3. 字体与排版 (Typography)
*   **无衬线现代字体**：西文使用 `Inter` 或 `Roboto`，中文默认调用系统黑体（PingFang SC / 思源黑体）。
*   **超大字号原则**：**不要让用户在炒菜时眯着眼睛看手机！**
    *   当前步骤的动词（如：**切块**、**大火爆炒**）字号需达 `28pt-32pt`，并且加粗 (Font-weight: 700)。
    *   倒计时数字字号需达 `48pt` 以上，占据屏幕核心视觉区。

## 4. 核心交互原则 (Interaction Principles)
*   **防脏手/盲操区 (Touch Targets)**：
    *   “下一步”按钮不应是一个小圈，而应该是屏幕底部的**巨大点击热区**（甚至设定为点击全屏任意非按钮区域即视为“下一步”）。
*   **非线性视觉分布**：
    *   **当前焦点（现在做什么）**：高亮、居中、巨大。
    *   **并行支线（顺手做什么）**：悬浮在焦点下方，使用次要颜色。
    *   **未来预期（下一锅放什么）**：在底部边缘半透明露头，给用户心理准备。
*   **时序声音强化**：
    *   倒计时最后 10 秒加入“滴答”声。
    *   步骤完成瞬间给予清晰清脆的“叮”声反馈。

---
*注：我将基于此规范，立即为您开发一个可互动的高保真前端原型（HTML/CSS/JS）。*
